---
ts: 2026-06-03T14:47:31+09:00
session_id: 85d38725-71ab-4550-a099-9ae5ee01ca58
cwd: C:\Users\Owner\business\brain\scripts
host: 大井
event: UserPromptSubmit
chars: 8849
tags: [conversation, raw-prompt]
---

# 発話 2026-06-03 14:47

```
<task-notification>
<task-id>w4q6f3lzh</task-id>
<tool-use-id>toolu_01U4Pm6fsZnN42Sanq4NcrtR</tool-use-id>
<output-file>C:\Users\Owner\AppData\Local\Temp\claude\C--Users-Owner-business\85d38725-71ab-4550-a099-9ae5ee01ca58\tasks\w4q6f3lzh.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Scrutinize whether the 8GB/no-admin brain+OpenClaw system is now truly optimal/strongest, verify applied fixes are live, find remaining concrete optimization gaps" completed</summary>
<result>{"plan":{"overall_verdict":"短い日本語テスト。","top_optimizations":[{"rank":1,"title":"NUM_PARALLEL=1とMAX_LOADED_MODELS=1へ降格(env根治で即GPU100化)","why":"ollama psが25/75 CPU GPUを示しKVがnum_ctxの4倍確保されレイヤがCPUへ退避し全ジョブが遅い。実質1ジョブ直列なので並列スロット4個は未使用で速度低下だけ生む。13修正がアプリ層で回避する根本原因がenvに残る最大の穴。","fix":"User環境変数NUM_PARALLELを1とMAX_LOADED_MODELSを1にしollama app再起動。検証はollama psがPROCESSOR100GPUとGPULayers37。","effort":"quick","risk":"低。並列1で逐次化するが現状もVRAM枯渇で事実上逐次のため実害ほぼ無し。可逆。"}],"dashboard_recommendations":["GPUフィット指標の常時可視化。ollama psのCPU GPU比を表示。"],"leave_as_is":["queue_guardの自己修復群は無停止運用の中核で触らない方がよい。"]},"raw":[{"dimension":"スループット最適化（8GB GPUでのqwen3:8b 処理速度・並列度の最大化）","verdict":"現状は最適でない。最大のボトルネックは OLLAMA_NUM_PARALLEL=4 で、ollama 0.24.0 は KVキャッシュを num_ctx×並列数 で確保するため(server.log: KvSize=8192×4=32768、巨大プロンプト時は131072)、qwen3:8b の GPUレイヤが 12〜31/37 に削られ最悪 6.4GiB KVがCPUへ退避→部分CPU推論で激遅化している(ollama ps=23%/77% CPU/GPU、nvidia-smi=7691/8151MiB使用・空き206MiB)。しかしワーカーはレーン別ロックで実質1ジョブ直列なので並列スロットは1個も使われておらず、NUM_PARALLEL=4は速度を犠牲にして何の利益も生んでいない。keep_alive=30m/FLASH_ATTENTION/q8_0 KV は適切で、再ロードも夜間ほぼ0で問題なし。直列1ジョブ設計自体は8GB制約下では妥当だが、NUM_PARALLEL=1化で全37レイヤをGPUに乗せれば同じ直列でも1ジョブが大幅高速化する。","gaps":[{"title":"OLLAMA_NUM_PARALLEL=4 が KV を4倍確保し GPU レイヤを CPU に追い出している(最重要)","severity":"high","evidence":"server.log runner.go:1297 'Parallel:4 ... KvSize:131072(=8192x16) GPULayers:12' および 'KvSize:32768(=8192x4) GPULayers:31'。device.go:251/256 'kv cache CUDA0 3.2GiB / CPU 6.4GiB'。ollama ps='qwen3:8b 8.1GB 23%/77% CPU/GPU'。nvidia-smi='7691/8151MiB used, free 206MiB'。User env: OLLAMA_NUM_PARALLEL=4。worker.ps1 はレーン別 .worker-&lt;lane&gt;.lock(L17,60-69,96)で1ジョブ直列のため並列スロットは未使用。","current":"NUM_PARALLEL=4 で ollama が KV を num_ctx×4 (巨大ctx時は×16相当=131072) 確保→KVが最大9.6GiB要求され 6.4GiB が CPU に溢れ、qwen3:8b の37レイヤ中12〜31しか GPU に乗らない。ワーカーは実際には1レーン1ジョブしか走らせないので、この4並列は速度低下のみで恩恵ゼロ。","better":"User環境変数 OLLAMA_NUM_PARALLEL を 1 に変更([Environment]::SetEnvironmentVariable('OLLAMA_NUM_PARALLEL','1','User'))→ollama再起動。KV確保が1/4(num_ctx=8192で約0.5GiB)になり、qwen3:8b 全37レイヤが8GBに収まり GPU 100%実行へ。直列1ジョブ設計のまま1ジョブ単価が短縮(特に12レイヤ落ちの最悪ケース170〜299s帯が解消)。検証は変更後に 'ollama ps' が 100% GPU、server.log の GPULayers:37 commit を確認。","risk":"並列1なので2レーン(heavy/light)が同時にollamaを叩くと2本目は待たされる(逐次化)。ただし現状もVRAM枯渇で事実上逐次なので実害は小。MAX_LOADED_MODELS=2 のまま2モデル同時要求が来ると再び溢れる→次項とセットで実施推奨。","effort":"quick"},{"title":"OLLAMA_MAX_LOADED_MODELS=2 が2モデル同時ロードを許し8GBを奪い合う","severity":"high","evidence":"User env OLLAMA_MAX_LOADED_MODELS=2。nvidia-smi 空き206MiBで余裕ゼロ。worker/dispatch は全経路 qwen3:8b 単一モデルに統一済(worker.ps1 L20,114-118 / dispatch.ps1 L175-184)。server.log の GPULayers が 12↔31↔22 と振れるのは別モデル(phi3/embed)やKV競合でVRAMが奪われた時の挙動と一致。","current":"実運用は qwen3:8b 単一なのに MAX_LOADED_MODELS=2。embed_wiki.ps1 等が nomic-embed や phi3 を呼ぶ瞬間に2モデルが同居し、qwen3:8b のレイヤが GPU から押し出され次の生成が部分CPU化する。","better":"OLLAMA_MAX_LOADED_MODELS=1 に変更。生成モデルは常に qwen3:8b 単独でVRAMを占有→レイヤ落ちが起きない。embed が必要な時は qwen3:8b を一旦アンロードして embed→再ロード(keep_alive内なら高速)で逐次化する方が、同居でずっと部分CPU化するより速い。","risk":"embedと生成が交互だとモデルスワップ(再ロード~2〜3秒×頻度)が増える。embed をまとめてバッチ実行する時間帯に寄せれば緩和可。embedは1.79〜2.67秒で再ロード(server.log llama runner started)なので影響は軽微。","effort":"quick"},{"title":"num_ctx=8192固定 + 57K文字プロンプトで巨大KV確保→prompt eval が支配的","severity":"high","evidence":"worker.ps1 L191,206 'num_ctx = 8192' 固定。worker-heavy.log の全ジョブが prompt_len=57000〜58717、duration avg=170s(最大299s)。これは8192トークン超でollamaが ctx を拡張し(server.log KvSize:131072)、KVがCPUへ溢れる最悪パターン。light(prompt 47〜6561)は avg=134s。dispatch.ps1 が prefix+template+context_files(各8000字, worker.ps1 L125)を無制限prependしているのが57K化の原因。","current":"全heavyジョブが ~57K文字=およそ20K〜28Kトークンを毎回投入。num_ctx=8192を超えるためollamaがctxを動的拡張し巨大KVを確保、GPUレイヤが12まで落ちる。prompt eval(入力処理)が出力生成より長く、これが170sの主因。出力out=1525〜5831と幅があるのに入力は常に巨大で固定的に重い。","better":"(1)dispatch.ps1で context_files 注入量を 8000字×N から 合計上限(例: 4000字×最大2件)に絞り、prompt-prefix も heavy系では短縮版を使う。(2)worker.ps1 で num_ctx をプロンプト長から段階制御(&lt;800字→2048, &lt;3000字→4096, それ以上→8192)。短いlightジョブのKVを縮めGPU占有率を上げ、生成を速くする。num_predict=4000も多くのジョブは1500〜3000出力なので 3000 へ下げて上限張り付き時の無駄生成を削減。","risk":"context注入を削ると参照不足で品質低下の懸念→重要タスクは -Priority super で従来通り全量注入する例外を残す。num_ctx段階化は境界値のジョブで稀に切り詰めが起き得るので、入力長がnum_ctxを超えたら1段上げるガードを入れる。","effort":"medium"},{"title":"ワーカーが1起動1ジョブで終了→1分スケジューラ再発火の大半がロック空振り","severity":"medium","evidence":"BrainWorkerHeavy/Light が Repeat=PT1M(schtasks)。worker-light.log/heavy.log に 'Skip: worker already running (lock age: 1〜5min)' が1ジョブあたり2〜5回。処理は5:00〜12:00で 18〜24件/時=2レーン合算で実効 約0.35件/分。1ジョブ134〜170sなのでレーンあたり理論 ~0.4件/分、ほぼ上限に張り付いており再発火は無益。inbox=31でバックログは小。","current":"1分ごとに起動→既存ロックを見て即exit、を1ジョブ完了まで繰り返す。CPU浪費は軽微だが、ジョブ完了〜次起動まで最大59秒の空白(完了直後にロック解放→次の1分起動を待つ)が生じ得る。実際 done のhour別は24件/時で頭打ち、これはGPUレイヤ落ちによる1ジョブ低速が律速で、ループ化しても並列1なら件数は伸びない。","better":"NUM_PARALLEL=1化でGPU100%にした後、worker.ps1 を『ロック取得→inboxが空 or 上限時間(例45分)までループで連続処理』する内部ループ化。完了直後の最大59秒アイドルを消し、再ロード/JITウォームを跨いで連続drainできる。ループ上限時間と .paused / inbox空で必ず抜ける条件を入れ、スケジューラはロック死活監視役に降格。","risk":"ループ中にOllamaが落ちると1ジョブ分の取りこぼし→worker内でOllama死活を各反復先頭で再確認し、失敗ならbreakしてロック解放。長時間ロック保持で queue_guard の stale lock(60分, worker.ps1 L62)と競合しないようループ上限を45分以内に。並列1のため件数増は限定的(主効果はアイドル除去)で、本命はあくまでGPUフィット改善。","effort":"medium"},{"title":"NumThreads:8 固定で16論理コアの半分しか使っていない(CPU溢れ時に効く)","severity":"low","evidence":"server.log 'NumThreads:8'。CPU=Ryzen7 5700X 物理8/論理16(Get-CimInstance Win32_Processor)。KVがCPUへ溢れる(6.4GiB)状況では一部レイヤがCPU実行されるため、CPUスレッド数が部分推論速度に直結する。","current":"ollamaは物理コア数=8をデフォルトスレッドにしている。GPUに全レイヤ載れば無関係だが、載りきらない過渡期や巨大ctx時はCPU実行分が8スレッドに制限され遅い。","better":"GPUフィット改善後も巨大プロンプトでCPU溢れが残る場合のみ、OLLAMA_NUM_THREADS=12〜14(物理8超〜論理16未満)を試す。SMTで頭打ちしやすいので12前後が現実的。ベンチで eval rate(tokens/s)を変更前後で比較し、改善が無ければ8へ戻す。","risk":"スレッドを上げすぎるとコンテキストスイッチでむしろ低下し、他プロセス(Chrome/Slack/Claude)を圧迫。GPU全載が達成できればそもそもCPU推論が消えるので、本項は第1・2項を実施しても巨大ctxが残る場合の補助に留める。","effort":"quick"}]},{"dimension":"qwen3:8b 出力品質の8GB制約下での最大化","verdict":"最適化されていない。13修正は「動かす(安定性/スループット)」を達成したが「出力の中身の質」はほぼ手付かず。実出力3件すべてが同一テンプレ反復・水増し(persona5件が本文ほぼコピペ、EEMUS20社が実質12団体の重複+全社同一定型文、SEO本文がAI臭の塊)で、これは prefix未活用 と context注入57k字をnum_ctx=8192が約85%切り捨てる構造欠陥に直結する。AI臭禁止表/部署別ルールを持つ v3 prefix は1スクリプトも参照しておらず、品質スコアラも反復崩壊を検出できない。精度最優先方針なのに「精度を上げる経路」が実質ゼロで、最強状態ではない。","gaps":[{"title":"context注入57k字を num_ctx=8192 が約85%切り捨て(instruction消失)。最大の品質殺し","severity":"high","evidence":"worker-heavy.log の prompt_len が一貫して57000-75000(median heavy帯), 全2754ジョブ中911件(33%)が24576字超。done JSON再構成で [Shincoder]/[BG-Competitive] 等は context_files=17個×~3145字=53471字。worker.ps1:206 num_ctx=8192。worker.ps1:131-134 は prompt(指示)を先頭・[参考資料]53k字を後ろに連結。Ollama 0.24.0 は超過時に末尾を残し先頭を捨てる→指示本体+先頭12-13個のentityが破棄され、モデルはentity羅列の末尾しか見ない","current":"57k字プロンプトの先頭(=タスク指示)が num_ctx=8192 で切り落とされ、モデルは何を作るべきか曖昧なまま末尾の参考資料だけで生成。これが反復・オフタスク・水増しの主因","better":"(A即効) worker.ps1 の context合算上限を num_ctx に合わせ約5000字(全context合計)へ絞る。1ファイル8000字×無制限→『全context合計 max ~4500字』に変更。(B) 連結順を逆転し [参考資料]→指示 ではなく『指示を最後に置く』(末尾優先のOllamaに指示を必ず残す)。worker.ps1:133 を $finalPrompt = \"[参考資料]$contextText`n`n# 厳守タスク`n$($job.prompt)\" へ。(C) num_ctx を 12288 へ引き上げ(8GB+qwen3:8b Q4で実測可能域、KVキャッシュ増分は数百MB)。精度最優先方針なら C+A+B 全採用","risk":"num_ctx引き上げはVRAM増→OOM再発リスク(8GBギリギリ)。まず12288で1ジョブ実測し ollama ps の SIZE/PROCESSOR がGPU100%維持を確認してから常用。context削減は情報量低下だが、現状は切り捨てられ無駄なので実質ノーリスク","effort":"quick"},{"title":"prefix v1 の18 wikilinkが dispatch.ps1 で17 entity全件を自動注入→上記57k肥大の発生源","severity":"high","evidence":"dispatch.ps1:131-144 は finalPrompt中の [[entities/xxx]] を正規表現で検出し対応ファイルを自動添付。prefix(v1)は dispatch.ps1:72 で全&gt;200字プロンプトに prepend され、prompt-prefix.md:20 に18個の[[entities/...]]を列挙。PowerShell実測: prefix由来で17 entity(53471字)が無条件添付される。短いshincoderプロンプト(2950字)が56421字に膨張する原因はこれ(shincoder_loop.ps1 は -ContextFiles を渡していない)","current":"タスクに無関係な事業まで含む全17 entityが毎回53k字添付され、num_ctx超過の固定原因になっている。タスクが Testall単体でも EcoKan/和コウチャイ等16個が無駄に注入される","better":"dispatch.ps1 の自動entity注入を『prefix由来のwikilinkは対象外』にする。具体: 自動注入の正規表現を prefix prepend前の元 $Prompt に対してのみ実行(line126で prefix結合する前に entityMatches を取る)。これで『タスクが明示参照したentityだけ』注入され、無関係16ファイルが消える。prefix側の[[]]一覧は人間可読の参照表として残してよい","risk":"意図的にprefixのentity全部を読ませたいケースがあれば情報減。だが現状は num_ctx で切られ機能していないため実害なし。むしろ関連entityだけ全文がctx内に収まり精度向上","effort":"quick"},{"title":"AI臭禁止表・部署別ルールを持つ prom
... (truncated 12334 chars, full result in C:\Users\Owner\AppData\Local\Temp\claude\C--Users-Owner-business\85d38725-71ab-4550-a099-9ae5ee01ca58\tasks\w4q6f3lzh.output)</result>
<usage><agent_count>4</agent_count><subagent_tokens>366221</subagent_tokens><tool_uses>130</tool_uses><duration_ms>4166543</duration_ms></usage>
</task-notification>
```
